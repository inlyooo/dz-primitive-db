"""Настройки файлов и допустимые типы данных."""

META_FILE = "db_meta.json"
DATA_DIR = "data"
VALID_TYPES = {"int": int, "str": str, "bool": bool}
ID_COLUMN = "ID"
ENCODING = "utf-8"
COMMAND_PROMPT = ">>>Введите команду: "

HELP = """***База данных***

Управление таблицами:
  create_table <таблица> <столбец:тип> ... — создать таблицу
  list_tables — показать список таблиц
  drop_table <таблица> — удалить таблицу
  info <таблица> — показать структуру и число записей

Операции с данными:
  insert into <таблица> values (<значение>, ...) — добавить запись
  select from <таблица> [where <столбец> = <значение>] — прочитать записи
  update <таблица> set <столбец> = <значение> where <столбец> = <значение>
  delete from <таблица> where <столбец> = <значение> — удалить записи

Типы: int, str, bool. Строки вводите в кавычках, bool — true или false.
ID генерируется автоматически. Для update и delete условие обязательно.
  help — справка
  exit — выход
"""
